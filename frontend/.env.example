VITE_SUPABASE_URL="https://YOUR_PROJECT_REF.supabase.co"
VITE_SUPABASE_ANON_KEY="YOUR_SUPABASE_PUBLISHABLE_OR_ANON_KEY"
# Optional — leave unset for same-origin /api (Vite dev proxy + nginx in Docker).
# Required for mobile testing on the same Wi‑Fi: do NOT use localhost here; either leave
# unset (recommended) or use your PC's LAN IP, e.g. http://192.168.1.10:5001
# VITE_API_BASE_URL="http://localhost:5001"