# Applied to repo files when you run: npm run env:local
# Secrets (DATABASE_URL, Supabase keys) are never changed — only mode-specific keys below.

@file backend/.env
PORT=5001
FRONTEND_URL=http://localhost:5173

@file frontend/.env
BACKEND_PORT=5001

@file .env
FRONTEND_URL=http://localhost
